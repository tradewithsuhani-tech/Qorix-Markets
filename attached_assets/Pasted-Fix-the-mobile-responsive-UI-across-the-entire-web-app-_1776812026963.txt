Fix the mobile responsive UI across the entire web app, especially the bottom navigation, notifications panel, and trade history screen.

Problems to solve:
1. On mobile, bottom navigation is covering page content and hiding text/cards at the bottom.
2. Notification drawer/popup opens downward and goes off-screen on mobile. It should open inside viewport and remain fully visible.
3. Trade history layout is cluttered on mobile and should be redesigned to look closer to the Exness-style mobile history:
   - clean grouped history by date
   - each trade in a compact card/row
   - large pair name
   - direction color-coded (Buy blue, Sell red)
   - entry text smaller
   - profit/loss aligned right and prominent
   - close price/time secondary
   - better spacing and typography
4. Improve all mobile screens so nothing is cut off, hidden behind navbar, or overflowing horizontally.
5. Ensure cards, buttons, charts, and popups scale correctly for mobile widths from 320px to 480px.

Required fixes:
- Add safe bottom padding to all scrollable pages so fixed bottom nav never overlaps content
- Use mobile-first responsive layout
- Fix notification panel position:
  - on mobile, open as top-right sheet or full-width dropdown inside viewport
  - max height with internal scroll
  - proper z-index
- Redesign trade history mobile UI inspired by Exness closed trades screen:
  - tabs: Open / Pending / Closed
  - date group headers
  - each row with pair, direction, lot/entry line, PnL on right, close price/time secondary
  - no cramped text
  - better alignment and spacing
- Make bottom nav icons/text smaller and balanced on mobile
- Respect iPhone/Android safe areas using env(safe-area-inset-bottom)
- Test on common mobile sizes:
  - 360x800
  - 390x844
  - 412x915
- Keep desktop/tablet UI unchanged

Technical requirements:
- If using React + Tailwind:
  - create reusable mobile layout helpers
  - add pb-[calc(80px+env(safe-area-inset-bottom))] or equivalent to page containers
  - use sticky/fixed nav carefully
  - avoid absolute positioning that breaks on small screens
- Refactor notification component for responsive positioning
- Refactor trade history component into a clean mobile card list
- Add overflow handling, line clamping, and spacing fixes

Expected result:
- Mobile web app should feel polished like a native trading app
- No hidden content behind bottom nav
- Notification popup fully visible
- Trade history clean, readable, premium, and Exness-inspired on mobile